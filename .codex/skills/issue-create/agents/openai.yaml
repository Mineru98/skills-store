interface:
  display_name: "Issue Create"
  short_description: "Draft and register a GitHub issue before starting work"
  default_prompt: "Use $issue-create to turn this change request into a GitHub issue that $issue-start can pick up."
